{
  "name": "model-loader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vite-gh-pages"
  },
  "devDependencies": {
    "vite": "^6.1.0",
    "vite-plugin-gh-pages": "^1.0.1"
  },
  "dependencies": {
    "three": "^0.173.0"
  }
}
